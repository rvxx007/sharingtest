import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:["avatars.slack-edge.com","certificates.simplicdn.net","sharingtest.onrender.com"]
  }
};

export default nextConfig;
